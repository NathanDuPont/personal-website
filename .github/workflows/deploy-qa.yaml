on: 
  workflow_dispatch:
    branches:
      - master 
name: Deploy to QA
jobs:
  web-deploy:
    name: Deploy
    runs-on: ubuntu-latest
    steps:
    - name: Get latest code
      uses: actions/checkout@v2.3.2
    
    - name: Upload files
      uses: SamKirkland/FTP-Deploy-Action@4.0.0
      with:
        server: ${{ secrets.qa_ftp_url }}
        username: ${{ secrets.qa_ftp_username }}
        password: ${{ secrets.qa_ftp_password }}