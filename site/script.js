window.onload = function() { //Custom scaling for header and content
    var menuItems = document.getElementById("navbar").getElementsByTagName("li"); // Get number of tabs present 
    for (let tab of menuItems) {
        console.log("test");
    }
};